Each computer needs an identifier to communicate over a network 
so  IP Address 
In IP max you can have is 2^8.2^8.2^8.2^8 so not enough ip addresses 
so we use NAT Network address translator <- Why need to use 
  so mutiple systems have the same IP so NAT kinda remembers which system IP sends what req


VPC spans accross different availability zones but mutiple subnets can sit inside the same availablity zone 

you can connet VPC ti VPC using VPC peering 
- something called private Link were you can connect your private router to the VPC
- or you assign VPC endpoint to tell the VPC to access this to go outside to S3 but more expensive to go outside preffered to keep coms within the VPC
  
Entire FLOW :

OUTSIDE {INTERNET}
        {S3}
____________________________        
VPC: 10.0.0.0 / 24

  AvZo 1:
    -> EC2 instance, SSH
        Security Group 3 (SG3)
    _______________    
    -> SN-1 Public
        SG1 
        Route Table:
        10.0.0.0 / 24 - Local
        0.0.0.0 / 0   - Internet Gate Way 
     _______________   

  [IGW]
     _______________
    -> SN-2 Pvt
        SG2
        Route Table:
        10.0.0.0 / 24 - Local
        0.0.0.0 / 0   - NAT Gate Way
      ______________
  AvZo 2:
    SN-1
    SN-2
____________________________

If you dont do anything Subnet becomes private
From the internet you can access SN-1 in AvZo 1 cause its public 

But keeping a subnet completely restricted is good in terms of security but not functionality 
For E.g if you want to install a software Update u need to access the internet 
  -> how is this possible ?
    So inside the Pvt SN-2 create a RouteTable (by default in route table 10.0.0.0 /24 will be Local ) it also has 0.0.0.0/0 means any address 
    all req comes in SN and SN route table decides what to send where 
    say we send req from SN-2 to google.com (interent) for that we have a special device called **NAT gateway** which gives interent access to private SN so in SN-2
    you can put 0.0.0.0/0 - NAT Gateway (NAT gateways only handles for req outiside i.e. internet)
    IMPORTANT : EXAM Q they ask which sys has what permission and what not and needs NAT GATEWAY

Now from SN-1 how to communicate to SN-2
  We have a special device called **INTERNET gateway** diff from NAT?
    this one communicate inside the AvZo and outside to internet 

How to improve more security
in Public you can create EC2 instance and open a SSH on port 22 (check beston)
   that way you dont handle or show ports from inside the subnet and they are seerate liek the front entrance security 
